# PolyMaker - Variables de entorno requeridas
# Copiar este archivo como .env y llenar los valores

# === OBLIGATORIAS ===

# Clave privada de tu wallet de Polymarket (sin prefijo 0x)
POLYMARKET_PRIVATE_KEY=tu_clave_privada_aqui

# URL de conexión a PostgreSQL (se genera automáticamente con deploy.sh)
DATABASE_URL=postgresql://polymaker:TU_PASSWORD_AQUI@localhost:5432/polymaker

# Secreto para sesiones del dashboard (genera uno aleatorio)
SESSION_SECRET=genera_un_string_aleatorio_largo_aqui

# === OPCIONALES ===

# Puerto del servidor (default: 5000)
PORT=5000

# Entorno (production para el servidor)
NODE_ENV=production

# Si usas una wallet con funder address (proxy wallets)
# POLYMARKET_FUNDER_ADDRESS=0x...
